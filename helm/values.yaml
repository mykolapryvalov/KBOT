# Default values for helm
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  registry: "ghcr.io"
  repository: mykolapryvalov
  tag: "v1.1.0-4cc19ef"
  arch: amd64
  os: linux
  pullPolicy: IfNotPresent

secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"

securityContext:
  privileged: true  

service:
  port: 80